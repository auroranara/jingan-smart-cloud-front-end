import React from 'react';

import FcSection from './FcSection';

export default function UnitLookUpReverse(props) {
  const { isBack } = props;

  return (
    <FcSection title="单位查看反面" isBack={isBack}>
    </FcSection>
  );
}
